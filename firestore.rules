
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enterprise access logic
    match /{document=**} {
      allow read, write: if true; // In production, refine this to request.auth != null
    }
  }
}
