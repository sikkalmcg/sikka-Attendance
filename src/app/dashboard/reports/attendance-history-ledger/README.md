Attendance History Ledger module

- API: /api/reports/attendance-history-ledger
- UI page: /dashboard/reports/attendance-history-ledger

Export:
- CSV supported via export=true&format=csv

Print:
- print=true returns { meta, rows }
- UI renders print-friendly HTML

Note: Excel/PDF export + advanced working-hours calculation are pending refinement.
